//Need to swap it maybe later 
#include <stdio.h>
/*
#define N 4

void ArrayRead(char ar[], int size);
void PrintArray(char ar[], int size);

int main() {
	char ListChar[N], exit;
	printf("Please enter %d characters: ", N);
	do {
		ArrayRead(ListChar, N);
		PrintArray(ListChar, N);
		printf("would you like to exit the loop press e;\n");
		rewind(stdin);
		scanf("%c", &exit);
	} while (exit != 'e');
	return 0;
}

void ArrayRead(char ar[], int size){
	int i;
	char temp;
	for (i = 0; i < size; ++i) {
		if ("Scanf Value: %d\n", scanf("%c", &temp) == 1){
				ar[i] = temp;
			} else {
				printf("Some error accured.\n");
				return;
			}
		}
	return;
}

void PrintArray(char ar[], int size){
	int i;
	for (i = 0; i < size; ++i) {
		if(ar[i] != '0')
		printf("index: %d value: %c\n", i, ar[i]);
	}
	return;
}*/
