#include <stdio.h>
#include <conio.h>
#include <math.h>
/*
int main()
{
	double x, a, b; //creating varibles
	printf("Given the equation a^x=b \nEnter a and b, repectively please: "); //priniting request
	scanf("%lf%lf%", &a, &b); //getting request
	x = log(b) / log(a); //calculating
	printf("x is: %g", x); // printing result
	getch();
	return 0;
} 
*/