#include <stdio.h>
#include <math.h>
#include <conio.h>
/*
int main()
{
	int sequence, digit1, digit2, digit3, digit4;
	printf("Enter a positive 4 digit number, please:");
	scanf("%d", &sequence);
	if (sequence < 1000 || sequence > 9999)
	{
		if (sequence < 0) printf("%d is a negative number.",sequence);
		else printf("%d is not a 4 digits number.", sequence);
		printf("Bye bye.");
	}
	else
	{
		digit4 = sequence % 10;
		sequence /= 10;
		digit3 = sequence % 10;
		sequence /= 10;
		digit2 = sequence % 10;
		sequence /= 10;
		digit1 = sequence % 10;
		sequence /= 10;
		//printf("-%d-%d-%d-%d", digit1, digit2, digit3, digit4); //check the digit
		if ()
		{
			
		}

	}
	getch();
	return 0;
}*/